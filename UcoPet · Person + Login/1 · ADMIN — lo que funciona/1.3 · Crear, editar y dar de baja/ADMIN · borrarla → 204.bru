meta {
  name: ADMIN · borrarla → 204
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/persons/{{creadaId}}
  body: none
  auth: inherit
}

script:post-response {
  test("204", () => expect(res.getStatus()).to.equal(204));
}

settings {
  encodeUrl: true
}
