meta {
  name: ENUM · en la URL, valor invalido → 400
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/persons?documentType=NOEXISTE&documentNumber=71884203
  body: none
  auth: inherit
}

params:query {
  documentType: NOEXISTE
  documentNumber: 71884203
}

script:post-response {
  test("400", () => expect(res.getStatus()).to.equal(400));
}

settings {
  encodeUrl: true
}
