meta {
  name: ENUMERACION · 2 de 5
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/persons/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{correoInventado}}",
    "password": "loQueSea"
  }
}

script:post-response {
  test('401', () => expect(res.getStatus()).to.equal(401));
}

settings {
  encodeUrl: true
}
