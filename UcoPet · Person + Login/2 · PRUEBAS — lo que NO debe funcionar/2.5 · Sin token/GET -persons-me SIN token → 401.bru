meta {
  name: GET /persons/me SIN token → 401
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/persons/me
  body: none
  auth: none
}

script:post-response {
  test('401', () => expect(res.getStatus()).to.equal(401));
}

settings {
  encodeUrl: true
}
