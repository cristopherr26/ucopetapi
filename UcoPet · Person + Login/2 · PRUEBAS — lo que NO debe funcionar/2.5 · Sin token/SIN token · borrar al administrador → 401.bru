meta {
  name: SIN token · borrar al administrador → 401
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/persons/{{adminId}}
  body: none
  auth: none
}

script:post-response {
  test("401", () => expect(res.getStatus()).to.equal(401));
}

settings {
  encodeUrl: true
}
