meta {
  name: ENUM · lista de tipos de documento, SIN token → 401
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/persons/document-types
  body: none
  auth: none
}

script:post-response {
  test("401 — ya no es publica", () => expect(res.getStatus()).to.equal(401));
}

settings {
  encodeUrl: true
}
