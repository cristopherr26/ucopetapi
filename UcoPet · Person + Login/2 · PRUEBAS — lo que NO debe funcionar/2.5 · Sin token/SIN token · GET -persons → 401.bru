meta {
  name: SIN token · GET /persons → 401
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/persons
  body: none
  auth: none
}

script:post-response {
  test("401", () => expect(res.getStatus()).to.equal(401));
}

settings {
  encodeUrl: true
}
